#edit.html.erb

<h1> Editing post </h1>
<%= form_for :post,url:post_path(@path), method: :patch do |f| %>
<% if@post.errors.amy? %>
<div id="error_explanation">
<h1> % =pluralize(@post.errors.count, "error")%> prohibited this post from being saved: <h2>
	<ul>
		<% @post.errors.full_messags.each do |msg| %>
		<li> <%= msg > </li>
		<% end %>
	</ul>
</div>
<%end>
<p>
<%= f.label :title %> <br>
<%= f.text_field :title %>
</p>

<%= f.label :text %> <br>
<%= f.text_area :text %>
</p>

<p>
	<%=f.submit %>
</p>
<%end>
<%= link_to 'Back', posts_path %>
| <%= link_to 'Edit', edit_post_path(@post) %>